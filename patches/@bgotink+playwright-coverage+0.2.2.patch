diff --git a/node_modules/@bgotink/playwright-coverage/lib/data.js b/node_modules/@bgotink/playwright-coverage/lib/data.js
index 57156a8..b367dcd 100644
--- a/node_modules/@bgotink/playwright-coverage/lib/data.js
+++ b/node_modules/@bgotink/playwright-coverage/lib/data.js
@@ -28,12 +28,11 @@ function collectV8CoverageFiles(suite) {
     return files;
 }
 exports.collectV8CoverageFiles = collectV8CoverageFiles;
-const fetch = eval('import("node-fetch")');
 async function getSourceMap(url, source) {
-    const match = source.match(/\/\/# *sourceMappingURL=(.*)/);
+    const match = source.match(/\s\/\/# *sourceMappingURL=(.*)/);
     if (match == null) {
         try {
-            const response = await (await fetch).default(`${url}.map`, {
+            const response = await fetch(`${url}.map`, {
                 method: 'GET',
             });
             return (await response.json());
@@ -62,14 +61,15 @@ async function getSourceMap(url, source) {
                 return JSON.parse(dataString);
             }
             default: {
-                const response = await (await fetch).default(resolved.href, {
+                const response = await fetch(resolved.href, {
                     method: 'GET',
                 });
                 return (await response.json());
             }
         }
     }
-    catch {
+    catch (err) {
+		console.log(err);
         return undefined;
     }
 }
